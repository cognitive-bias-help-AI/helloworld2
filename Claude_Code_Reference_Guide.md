# Claude Code 활용을 위한 핵심 프롬프트 및 시스템 가이드라인

이 문서는 Claude Code 에이전트가 복잡한 코드베이스에서 작업을 수행할 때 반드시 준수해야 하는 아키텍처 제약 사항, 워크플로우, 그리고 컨텍스트 관리 지침을 정의합니다.

## 1. 컨텍스트 및 세션 관리 (Context Management)
* **Dumb Zone 회피 및 상태 압축**: 컨텍스트 윈도우 사용량이 40~60%에 도달하면 인지 능력이 급격히 저하되고 코너를 자르는(Cutting corners) 현상이 발생하므로, 즉각 현재 세션을 중단해야 합니다. 중단 전 진행 상황, 해결된 문제, 남은 오류, 향후 목표를 `00-status.md` 문서로 요약(압축)하고 깨끗한 새 세션에서 해당 문서만 읽어들여 작업을 재개하십시오.
* **Lost in the Middle 방지**: 에이전트에게 관련 없는 방대한 빌드 로그나 검색 결과를 무분별하게 컨텍스트 중간에 주입하지 마십시오.

## 2. QRSPI 4단계 게이트 워크플로우
에이전트는 코드를 단 한 줄도 작성하기 전에 기획과 구조 설계에 지능을 집중해야 하며, 각 단계를 넘어가기 위해 인간 엔지니어의 명시적 승인(Explicit Approval)을 받아야 합니다. 전체 채팅 내역이 아닌 마크다운 파일로 의사결정을 기록하십시오.
* **Gate 1: 제품 및 기획 (Product / Questions & Research)**
  - 기술적 아키텍처 논의를 철저히 배제하고 비즈니스 요구사항과 성공 측정 지표만 정의합니다.
  - 순수 HTML 목업이나 '피처 출시 전 블로그 포스트' 형태로 결과물을 작성합니다.
* **Gate 2: 아키텍처 조사 (Architecture / Research)**
  - 에이전트에게 최종 피처 티켓(구현 목표)을 숨긴 채 기존 코드베이스의 객관적인 '팩트(Facts) 맵'을 구축합니다. 목표를 미리 알게 되면 발생하는 편향을 방지합니다.
* **Gate 3: 프로그램 설계 (Program Design / Structure & Plan)**
  - 파일 경로, 인터페이스, 메서드 시그니처, 예상 콜스택 등 미시적 구조만 설계하며, 메서드 본문(Body) 로직은 절대 작성하지 않습니다.
  - 설계 시 "결정 중 가장 확신이 없는 것이 무엇인가?"를 명시적으로 질문하여 암묵적 가정을 타파합니다.
* **Gate 4: 수직적 슬라이스 구현 (Vertical Slices / Implement)**
  - 데이터베이스부터 프론트엔드까지 한 번에 구현하는 수평적 개발을 엄격히 차단합니다. 5~10개의 핵심 요약만 제시하고 승인을 획득한 후 코딩에 돌입합니다.

## 3. 구현 및 테스트 세부 규칙
* **종단간 예광탄 (Tracer Bullet)**: 복잡한 비즈니스 로직을 배제하고 하드코딩된 Mock 데이터만 반환하는 텅 빈 API 엔드포인트와 껍데기 프론트엔드를 최초로 구축하여 브라우저나 curl로 구동을 증명하십시오.
* **해피 패스 및 점진적 확장**: 껍데기 파이프라인이 승인되면 목업 데이터를 제거하고 단 하나의 '해피 패스(정상 시나리오)' 로직만 주입합니다. 이후 슬라이스는 하나의 비즈니스 규칙, 에러 핸들링 등 원자 단위(Atomic)로만 점진적으로 확장합니다.
* **진짜 테스트 전용 (Real Tests Only)**: 새로 작성한 테스트 코드는 수정 전(Pre-patch) 원본 코드에서 실행 시 반드시 실패(Fail)를 반환해야만 유효한 테스트로 인정됩니다.

## 4. Andrej Karpathy 4대 원칙
1. **Think Before Coding**: 추측하지 말고, 혼란을 숨기지 말며, 트레이드오프를 수면 위로 끌어올리십시오.
2. **Simplicity First**: 문제를 해결하는 최소한의 코드만 작성하고 추측에 기반한 오버엔지니어링을 피하십시오.
3. **Surgical Changes**: 반드시 건드려야 할 부분만 만지고 자신이 어지럽힌 부분만 청소하십시오.
4. **Goal-Driven Execution**: 명확한 성공 기준을 먼저 정의하고 자동화된 테스트를 통해 이를 증명하십시오.

## 5. 아티팩트 및 시스템 제어 (Control Plane)
* **CLAUDE.md**: 프로젝트 전역의 '선언적 지식'만 포함하며, 컨텍스트 밀도 유지를 위해 가급적 300줄 미만으로 유지하십시오.
* **SKILL.md**: 
  - `name`: 1~64자의 소문자 알파벳, 숫자, 하이픈(-)만 허용됩니다.
  - `description`: 1024자 이내로 작성하되, XML 꺾쇠괄호(`< >`)는 시스템 프롬프트를 오염시키므로 절대 사용을 금지합니다.
  - 본문에는 구체적인 행동 규칙, 제한 권한, I/O JSON 구조 등 실제 코드 패턴 예시를 제공하십시오.
* **Lifecycle Hooks (`settings.json`)**:
  - `PreToolUse`: 파괴적인 Bash 쉘 명령어(예: `rm -rf`)가 감지되면 물리적으로 차단합니다.
  - `PostToolUse`: 파일 수정 완료 후 린터 및 포맷터를 백그라운드에서 자동 실행합니다.
  - `Stop`: 세션 종료 전 단위 테스트를 구동하고, 실패 시 에이전트가 에러 로그를 읽고 자가 수정(Self-Correction)하도록 유도합니다.
